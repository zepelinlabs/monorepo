version: "3.7"

services:
  landing:
    build: ./landing
    command: npm run dev
    env_file: "./landing/.env"
    ports:
      - 3000:3000
    volumes:
      - ./landing/code/:/code
      - /code/node_modules
      - /code/.next

